import { system, console } from "symbol"

var foo: string | null (string, bool) = (first: string, second: bool = false) {
    if (second == false) return null
    return null
    return first
}

var check = foo("bar")
if (check) system.quit(0)
check = foo("zoo", true)
console.writeln("check:", check)
