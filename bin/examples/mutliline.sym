import { console } from "symbol"

var main: null (args: string[]) = (args: string[]) {
    var foo: string = "42.0

42"
    var bar = '6969'
    // var bar: float = 420.0
    console.writeln("Hello, world! ${foo}") // , ${bar}
    console.writeln("bar: ${bar}") // , ${bar}
}

main([])
