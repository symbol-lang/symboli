import { console } from "symbol"

var main: null (args: string[]) = (args: string[]) {
    var foo: bool = true
    // var bar: float = 420.0

    if (foo == false) {
        console.writeln("Hello, world!")
    } else {
        console.writeln("ops")
    }
}

main([])
