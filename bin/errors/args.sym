import { console } from "symbol"

// Должно вызывать ошибку так как используется тип int[],
// при том что в args (первый аргумент, который должен иметь тип int[]) подается
// строка "40"
var main: (int[]) = (args: int[] = [10, 20, 30, "40"]) {
    console.writeln("Hello, world! ${args}")
}

main()
